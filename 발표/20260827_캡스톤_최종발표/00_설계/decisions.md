# 결정과 이유

> 왜 이렇게 짰는지를 남긴다. 다음 발표 때 같은 고민을 다시 하지 않기 위해서다.

| 날짜 | 결정 | 이유 | 검토했으나 버린 것 |
|---|---|---|---|
| 2026-08-27 | 발표 폴더 생성 | — | — |
| 2026-08-27 | **목적을 「결과 전달」이 아니라 「방법 이전」으로 잡음** | 청중이 자기 분석에 가져갈 수 있는 것은 일본 관광 결과가 아니라 대표점·공간 단위 사례. 결과만 전달하면 캡스톤 발표가 보고가 된다 | 「일본 관광 접근성 지도를 만들었다」로 잡는 안. 듣고 나서 할 일이 없어 폐기 |
| 2026-08-27 | **대표점 슬라이드(#8)를 한계가 아니라 클라이맥스로 승격** | 이 사례는 데이터·모형을 그대로 두고 결과만 두 배 바뀐 것을 보여준다. 방법론 발표에서 가장 비싼 자산 | 「한계」 슬라이드 안의 불릿 한 줄로 처리하는 안 |
| 2026-08-27 | **역인과를 #5에서 발표자가 먼저 말함** | 프로젝트 `CLAUDE.md` §1. 질의에서 지적당하면 방어가 되고, 먼저 말하면 설계가 된다 | 마무리에서 한 줄로 붙이는 안. 늦으면 이미 인과로 들은 뒤다 |
| 2026-08-27 | **결정계수 0.377 의 출처를 화면에 적음** (`파일에 없음 — analyze.py 콘솔값`) | `_meta/claims.csv` claim-01 등급이 `근거부족`. 숨기면 질의에서 무너지고, 적으면 자료 관리 능력의 증거가 된다 | 값만 쓰고 넘어가는 안 |
| 2026-08-27 | **근거 id 를 그림이 아니라 `lineage.csv` 산출물 경로로 인용** | `figures/fig-0*.svg` 는 부모 대장에 없다. 그림은 산출물의 표현이고 검증 대상은 산출물 | `fig-01` 로 인용하는 안 → `근거_미연결` 경고 7건 발생 |
| 2026-08-27 | **claims.csv 는 claim-02·03·04 만 인용** | 나머지는 `근거대기`·`근거부족`. 규칙상 등급미달 인용은 오류이고, 규칙이 옳다 | claim-05(27~32개)·claim-09(대표점) 인용 → `routes.csv`·`panel.csv` 로 대체 |
| 2026-08-27 | **김해 9월 44.1%를 본 덱에서 뺌** | README 가 스스로 원인 미확인이라고 적었다. 무대에서 꺼내면 원인 질문을 자초하고, 답이 없다 | 계절 슬라이드(#9)에 붙이는 안 → 백업 #12 로 |
| 2026-08-27 | **독도 처리를 본 덱 불릿 한 줄 + 백업 한 장으로 나눔** | 한국 청중에게 일본 경계 지도를 띄우면서 언급하지 않는 것이 더 위험하다. 다만 `claim-08` 이 코드 경로 불일치를 지적하므로 「경계를 읽는 경로에서 뺐다」까지만 단정 | 본 덱에 한 장 배정하는 안(10분에 안 맞음) / 아예 빼는 안(위험) |
| 2026-08-27 | **`배치 전면` 을 지도 슬라이드에 쓰지 않음** | `_base.css` 의 `l-full` 이 `object-fit: cover` 다. 1400×1000 그림을 16:9 에 넣으면 위아래가 잘리고, 홋카이도가 화면 밖으로 나간다 | 지도 두 장(#4·#6)을 전면으로 쓰는 안 → `기본`(contain) 으로 |
| 2026-08-27 | **`배치 강조` 를 #7 한 장에만 씀** | 라이브러리 README: 자주 쓰면 강조가 아니라 배경이 된다. 그림 없는 슬라이드가 #7·#10·#11 셋뿐이라 리듬 전환 자리로 #7 을 골랐다 | #11 마무리에도 강조를 쓰는 안 → `인용` 으로 |
| 2026-08-27 | **원고를 시간에 맞춰 늘림 (6장)** | 첫 판이 배정의 60~80%였다. 짧은 원고는 「그림 보며 즉흥」이 되고, 즉흥은 시간을 넘긴다. 늘린 내용은 전부 **일본 지리 설명** — 청중이 모른다고 `audience.md` 에 적은 것 | 배정 시간을 줄이는 안 → 총합이 540초 아래로 내려가 시간 오류 |

## 시간 배분

배정 600초 · 계획 590초(98%) · 본 슬라이드 10장 / 분당 1.0장.

가장 긴 두 장(#5 역인과, #8 대표점)에 140초, 전체의 24%를 넣었다.
**둘 다 「이 발표가 무엇을 주장하지 않는가」에 쓰는 시간이다.**
결과를 늘어놓는 데 쓰지 않은 것이 이 덱의 성격이다.

## 다음 발표 때 다시 볼 것

- `fig-06` 의 무게중심 계열이 어느 산출물에도 없다. 그림 생성 코드가 그때그때 다시
  계산하는 것으로 보이나 확인하지 않았다. **대장에 등록하거나, 등록 못 하면 그림을 바꾼다**
- `analyze.py` 가 결정계수를 파일로 저장하도록 고치면 다음 발표에서 이 고백이 필요 없다
- 2024년 자료로 잔차의 연도 안정성을 대조하면 「한 해짜리 아니냐」는 질문이 사라진다
