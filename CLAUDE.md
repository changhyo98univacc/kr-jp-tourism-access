# CLAUDE.md — kr-jp-tourism-access

한국 4개 공항(인천·김포·김해·제주) 기준 **일본 지역 접근성**을 계산하고,
실제 한국인 숙박자 수와 대조해 **불일치를 드러내는** Streamlit 앱.

상위 규약은 `AgentArmory/CLAUDE.md` 다. **여기에는 그 프로젝트 고유의 것만 적는다.**
절대원칙(삭제 금지·계획 선행·전량 복구·반출 승인)은 상속받으므로 다시 쓰지 않는다.

## 1. 이 프로젝트가 주장하는 것 / 주장하지 않는 것

- **주장하지 않는다: 인과.** 접근성과 방문객은 양방향이다(수요가 있어서 노선이 생긴다).
  "접근성이 관광객을 늘린다"고 쓰지 않는다.
- **주장한다: 불일치.** 접근성과 실제 방문의 **잔차 지도**가 산출물이다.
- 이 구분을 흐리는 문장을 코드 주석·앱 문구·발표자료 어디에도 쓰지 않는다.

## 2. 계산은 로컬, 앱은 조회

Streamlit Cloud 무료 티어는 메모리가 빠듯하고 접속마다 재실행된다.

- 무거운 계산(접근성 격자, 회귀, 잔차)은 `src/` 스크립트가 **로컬에서 미리** 돌린다
- 결과는 `data/processed/` 에 작은 파일로 저장하고 **그것만 커밋**한다
- `app/` 은 읽어서 보여주기만 한다. 앱 안에서 API를 호출하지 않는다

## 3. 데이터 취급

| | 규칙 |
|---|---|
| `data/raw/` | **커밋하지 않는다.** 출처 URL·획득일·SHA-256·재획득 방법을 `docs/DATA_SOURCES.md` 에 남긴다 |
| `data/processed/` | 커밋한다. 작고 재생성 가능해야 한다 |
| 자격증명 | **저장소에 절대 들어가지 않는다.** e-Stat appId, 공공데이터포털 키는 로컬 수집 스크립트에서만 쓴다 |
| 기준시점 | 모든 데이터에 **어느 시점 기준인지** 명시한다. 노선은 자주 바뀌고 통계는 개정된다 |

**코로나 구간(2020~2022)은 기본 분석에서 제외한다.** 2019 또는 2024 기준을 쓰고, 어느 쪽인지 밝힌다.

## 4. 일본 데이터 함정 (반드시 확인)

- **인코딩** — 일본 정부 CSV·DBF는 Shift-JIS(CP932)가 흔하다. UTF-8로 열면 깨진다
- **좌표계** — 측지계 JGD2011. **평면직각좌표계가 19계(I~XIX)** 다. 거리·면적은 지역에 맞는 계를 골라야 하며, 위경도에서 직접 계산하지 않는다
- **시정촌 코드** — 헤이세이 대합병으로 코드가 바뀌었다. 시계열 조인 시 확인
- **공간 단위** — 도도부현 집계로는 안 보이는 것이 시정촌에서 보인다(예: 쓰시마). 단위를 바꿔보고 결과가 달라지면 그 사실을 기록한다

## 5. 알려진 한계 (숨기지 말고 앱과 발표에 명시)

- 숙박자 ≠ 방문자. 당일치기·크루즈·지인집이 빠지고, 숙박지와 관광지가 다르다
- 숙박통계 일부 표는 **종업원 10인 이상 시설**만 집계 → 지방 과소평가
- 항공만 넣으면 **해상 노선(부산–쓰시마)** 이 빠져 극단적 이상치가 생긴다
- 지상이동 시간은 초기에 근사값을 쓴다. 근사 방식과 그 한계를 앱에 표시한다
